import React from 'react';
import { Typography } from '@mui/material';

const ContactTypesPage: React.FC = () => (
  <Typography variant="h4">Типы контактов (заглушка)</Typography>
);

export default ContactTypesPage; 